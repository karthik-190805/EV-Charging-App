<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔐 Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVApp"
        tools:targetApi="28">

        <!-- 🚀 Entry Point -->
        <activity
            android:name=".RoleSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".user.StationDetailActivity"
            android:exported="false"
            android:label="Station Details" />


        <!-- 👤 User-side Activities -->
        <activity android:name=".user.LoginActivity" android:exported="false" />
        <activity android:name=".user.SignUpActivity" android:exported="false" />
        <activity android:name=".user.HomeActivity" android:exported="false" />
        <activity android:name=".user.FeaturesActivity" android:exported="false" />
        <activity android:name=".user.ForgotPasswordActivity" android:exported="false" />
        <activity android:name=".user.OtpActivity" android:exported="false" />
        <activity android:name=".user.NewPasswordActivity" android:exported="false" />

        <!-- 🖥️ Server-side Activities (all with _server suffix) -->
        <activity android:name=".server.LoginActivity_server" android:exported="false" />
        <activity android:name=".server.SignUpActivity_server" android:exported="false" />
        <activity android:name=".server.HomeActivity_server" android:exported="false" />
        <activity android:name=".server.ForgotPasswordActivity_server" android:exported="false" />
        <activity android:name=".server.OtpActivity_server" android:exported="false" />
        <activity android:name=".server.NewPasswordActivity_server" android:exported="false" />




    </application>
</manifest>
